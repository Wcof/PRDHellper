repos:
  - repo: local
    hooks:
      - id: prd-consistency-check
        name: PRD Consistency Check (warn)
        entry: scripts/check_consistency.sh . --mode=warn
        language: system
        pass_filenames: false
