# 模式：system

请基于业务上下文生成系统级 PRD。

要求：
1. 先判断产品定型。
2. 先输出结构化骨架，再补充各章节内容。
3. 功能章节需要包含架构、流程、状态、功能清单和模块说明。
4. 信息不足处标注 [TODO]。
5. 系统级正式产物写入 `docs/prd/system/`。
6. 如果文档中已经明确出现页面、路由、弹窗、抽屉、模式页或独立工作台，必须继续拆分并写入 `docs/prd/pages/`。
7. 不能只输出一个根目录总 PRD 文件就结束；若 `docs/prd/system/` 和 `docs/prd/pages/` 为空，则交付不完整。
